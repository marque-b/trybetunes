import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      ligth: '#C16A33',
      main: '#6D3B1D',
      dark: '#402311',
    },
    background: {
      default: '#fff',
    },
  },
});

export default theme;
